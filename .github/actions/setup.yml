name: setup
description: Setup and Checkout
runs:
  using: 'composite'
  steps:
    - name: Setup and Checkout
      uses: actions/checkout@v4.1.1
    - name: Setup Java JDK
      uses: actions/setup-java@v4.0.0
      with:
        java-version: '17'
        distribution: 'adopt'

      